app:
    resource: "@AppBundle/Controller/"
    type:     annotation

logout:
  path: /logout

easy_admin_bundle:
    resource: "@EasyAdminBundle/Controller/"
    type:     annotation
    prefix:   /admin
